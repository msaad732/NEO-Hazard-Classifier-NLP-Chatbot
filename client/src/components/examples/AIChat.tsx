import { AIChat } from '../AIChat';
import { PreviewShell } from './PreviewShell';

export default function AIChatExample() {
  return (
    <PreviewShell>
      <AIChat />
    </PreviewShell>
  );
}
